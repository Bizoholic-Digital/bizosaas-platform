FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir pika kafka-python

# Copy setup scripts
COPY setup-rabbitmq-queues.py .
COPY setup-kafka-topics.py .

# Make scripts executable
RUN chmod +x *.py

CMD ["/bin/bash"]
