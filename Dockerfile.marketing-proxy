FROM node:18-alpine

WORKDIR /app

# Install http-proxy
RUN npm install http-proxy

# Copy the proxy script
COPY marketing-proxy.js .

# Expose port 3000
EXPOSE 3000

# Start the proxy server
CMD ["node", "marketing-proxy.js"]